{
  "installCommand": "rm -rf node_modules && npm install",
  "buildCommand": "npm run build",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
